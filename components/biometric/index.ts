// Biometric authentication components
export { default as BiometricLoginButton } from './BiometricLoginButton';
export { default as BiometricSettings } from './BiometricSettings';
export { default as BiometricSetup } from './BiometricSetup';

// Biometric authentication hook (re-export from context)
export { useBiometricAuth } from '../../context/BiometricAuthContext';
